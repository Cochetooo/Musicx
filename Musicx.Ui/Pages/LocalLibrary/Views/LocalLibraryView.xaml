<UserControl x:Class="Musicx.Ui.Pages.LocalLibrary.Views.LocalLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             DataContext="{Binding LocalLibraryVm, Source={StaticResource Locator}}"
             mc:Ignorable="d"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="300" d:DesignWidth="300">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <DataGrid Grid.Row="0"
                  x:Name="SongDataGrid" ItemsSource="{Binding Songs}" 
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectedValue="{Binding SelectedSong}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding TrackNumber}" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration, Converter={StaticResource SecondsToTimeConverter}}" />
                <DataGridTextColumn Header="Genre" Binding="{Binding GeneratedGenreName}" />
                <DataGridTextColumn Header="BitRate (kbps)" Binding="{Binding BitRate}" />
                <DataGridTextColumn Header="SampleRate (Hz)" Binding="{Binding SampleRate}" />
                <DataGridTextColumn Header="Format" Binding="{Binding AudioFormat}" />
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="⬅️ Précédent" Command="{Binding PreviousPageCommand}" 
                    IsEnabled="{Binding CanGoPrevious}" Margin="5"/>
            <Button Content="Suivant ➡️" Command="{Binding NextPageCommand}" 
                    IsEnabled="{Binding CanGoNext}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
