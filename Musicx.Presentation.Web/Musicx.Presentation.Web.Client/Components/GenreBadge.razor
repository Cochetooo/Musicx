@using Musicx.Contracts.Dto.Responses

<Badge Padding="Padding.Is1.OnY.Is2.OnX" 
       Class="neumorphism-shadow-default montserrat" 
       Style="@GetColor(Genre)" 
       TextSize="TextSize.Small" 
       Margin="Margin.Is1" 
       TextColor="TextColor.White" 
       Pill>
    @Genre.Name
</Badge>

@code {

    [Parameter] public OutGenre Genre { get; set; } = null!;
    [Parameter] public bool Influence { get; set; }
    
    private string GetColor(OutGenre genre)
        => $"background-color: {genre.Color ?? "#80858a"};" +
           (Influence ? " opacity: 0.7; font-style: italic;" : "font-weight: bold;");

}