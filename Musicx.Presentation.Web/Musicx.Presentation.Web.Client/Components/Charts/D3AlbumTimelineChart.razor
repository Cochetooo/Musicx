@inject IJSRuntime JS

<div @ref="_containerRef" class="d3-album-timeline-chart-container"></div>

<style>
    .d3-album-timeline-chart-container {
        width: 100%;
        min-height: 360px;
        position: relative;
    }

    .album-timeline-tooltip {
        background: linear-gradient(135deg, rgba(21, 17, 38, 0.95), rgba(34, 23, 56, 0.95));
        color: #fff;
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 0.75rem;
        padding: 0.45rem 0.6rem;
        box-shadow: 0 14px 34px rgba(0, 0, 0, 0.32);
        font-family: Inter, sans-serif;
        max-width: 220px;
        z-index: 20;
    }

    .album-timeline-tooltip__title {
        font-size: 0.8rem;
        font-weight: 600;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .album-timeline-tooltip__rating {
        margin-top: 0.1rem;
        font-size: 0.7rem;
        color: rgba(255, 255, 255, 0.78);
    }
</style>